REQUIRED PYTHON PACKAGES TO INSTALL:
- json
- Pillow
- Flask
- requests

To install packages, in terminal enter line below:
pip install json Pillow Flask requests


RUNNING THE PROGRAM:

To run the program, in the terminal enter line below:
flask run

Then click the link generated in terminal